[package]
name = "calque-engine"
version.workspace = true
edition.workspace = true
license.workspace = true

# Mode concret uniquement pour l'instant : pas de dépendance à calque-space
# (implémenté en parallèle). Le mode symbolique (§5.3) la réintroduira.
[dependencies]
calque-model = { workspace = true }
ipnet = { workspace = true }
serde = { workspace = true }
